
get_filename_component(SIMPLE_APPS_DIR "${CMAKE_CURRENT_SOURCE_DIR}/src/simple" ABSOLUTE)


# generate links and executables for each source
foreach()
  add_executable()
  target_sources()
  target_link_libraries()
endforeach()
